// Q1

// It'll take two numbers and add them together. 
// The value of result will the sum of the numbers, in this case 10

function myFunction(num1, num2) {
    return num1+num2
}

const result = myFunction(5,5)
console.log(result)

//Did exactly what we expected!